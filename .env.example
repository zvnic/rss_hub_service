# =============================================================================
# RSSHub — пример переменных окружения
# Скопируйте в .env и заполните обязательные значения (минимум REDIS_PASSWORD).
# =============================================================================

# ------------------------------
# Основные настройки RSSHub
# ------------------------------
RSSHUB_PORT=1200
RSSHUB_NODE_ENV=production
RSSHUB_LOGGER_LEVEL=info

# ------------------------------
# Redis (обязательно задать REDIS_PASSWORD)
# ------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password
REDIS_DB=0

# ------------------------------
# Кэш
# ------------------------------
CACHE_TYPE=redis
CACHE_EXPIRE=300
CACHE_CONTENT_EXPIRE=3600

# ------------------------------
# Puppeteer (рендеринг JS-сайтов)
# Заполняется автоматически при запуске с docker-compose.browserless.yml.
# Иначе оставьте пустым или укажите свой ws://хост:порт.
# ------------------------------
PUPPETEER_WS_ENDPOINT=

# ------------------------------
# Прокси (HTTP/HTTPS; для SOCKS5 нужен отдельный контейнер/сервис)
# PROXY_URI: протокол и хост, например http://proxy.example.com:8080
# ------------------------------
PROXY_URI=
PROXY_AUTH=

# ------------------------------
# Безопасность и CORS
# ------------------------------
ALLOW_ORIGIN=*
# Ограничьте домены при доступе с фронтенда: ALLOW_ORIGIN=https://myapp.com
ACCESS_KEY=

# ------------------------------
# Nginx (расширенная конфигурация)
# ------------------------------
NGINX_PORT_HTTP=80
NGINX_PORT_HTTPS=443
DOMAIN_NAME=rsshub.yourdomain.com
EMAIL=admin@yourdomain.com

# ------------------------------
# Prometheus (расширенная конфигурация)
# ------------------------------
PROMETHEUS_PORT=9090
METRICS_ENABLED=false
